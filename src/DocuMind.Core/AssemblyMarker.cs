namespace DocuMind.Core;

/// <summary>
/// Marker type for the DocuMind.Core assembly. Useful as a reference point for
/// assembly scanning (e.g. registering services or MediatR/validators) without
/// relying on a magic string. Domain models and abstractions live alongside it.
/// </summary>
public sealed class AssemblyMarker;
